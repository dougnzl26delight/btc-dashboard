# Core
ccxt>=4.0.0           # broker abstraction (Coinbase, Kraken, Binance, etc.)
python-dotenv>=1.0.0  # .env loading
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# Math / stats (matches stocks system)
scipy>=1.11.0
statsmodels>=0.14.0   # ADF cointegration test, etc.

# Visualization (optional, dev only)
matplotlib>=3.7.0

# ── Dashboard + cloud deploy (Streamlit Community Cloud) ──────────────
streamlit>=1.40.0           # the dashboard app itself
streamlit-autorefresh>=1.0.1 # auto-repaint the page every few min (always-on screen)
plotly>=5.20.0              # every chart + the pickled figures in .panel_cache
yfinance>=0.2.40            # equities / cross-asset / macro proxies in precompute
lxml>=5.0.0                 # html parsing used by yfinance / pandas.read_html

# Testing
pytest>=7.4.0
